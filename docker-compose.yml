version: '3'

networks:
  app-network:


services:
  app:
    image: node:18-alpine
    build:
      context: .
      dockerfile: ./Dockerfile
    ports: 
      - "4444:4444"
    networks:
      - app-network
    restart:
      unless-stopped
    expose:
      - "4444"